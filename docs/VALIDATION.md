# Проверка переноса — 22 сентября 2026

Основа: ZettelLocal 2.12.1 Setup Builder, 7 сентября 2026.

Проверка на Linux / Python 3.12 с FastAPI TestClient, отдельная временная SQLite-база:
- `/api/health`, `/`, `/static/app.js`: HTTP 200.
- Категории, сущности и граф сущностей: HTTP 200.
- Создание, изменение, чтение и удаление тестовой заметки: успешно.
- Экспорт ZIP: HTTP 200.

База из исходного архива не публикуется. Сборка оконного EXE больше не требует и не упаковывает `data/zettel.db`: приложение создаёт базу автоматически. Уточнена версия FastAPI с 2.11.0 на 2.12.1.

Ограничения: Windows EXE/установщик, визуальные сценарии редактора, качество Auto-Capture и подключение Ollama не проверены. Наличие кода функции не гарантирует её безошибочную работу во всех сценариях.
